# Database (Docker overrides this to sqlite:////app/data/flights.db)
DATABASE_URL=sqlite:///./flights.db

# Notifications via ntfy.sh
NTFY_TOPIC=lesgoski-default-topic
WEBAPP_URL=http://localhost:8000

# Scanner
SCAN_COOLDOWN_MINUTES=30
LOOKUP_HORIZON_DAYS=120

# Matcher
HOUR_TOLERANCE=1
NEARBY_AIRPORT_RADIUS_KM=100

# Scheduler
UPDATE_INTERVAL_MINUTES=180
FLIGHT_STALENESS_HOURS=24

# Auth
SECRET_KEY=change-me-generate-with-python-c-import-secrets-print-secrets-token-hex-32

# Admin account seeded on startup — change password after first login
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change-me-on-first-login

# Uvicorn (only relevant for Docker — leave defaults for local dev)
# UVICORN_HOST=0.0.0.0
# UVICORN_PORT=8000
# UVICORN_RELOAD=false
